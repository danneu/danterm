# Dev build output
build/
.build/
/result

# SwiftPM
.spm-build/
# The `just test` gate builds lib/TerminalCore with extra -Xswiftc args, which change
# SwiftPM's build-argument hash. Sharing .build/ with a bare `swift test` made the two
# invalidate each other on every switch; the gate gets its own scratch path instead.
.build-gate/
# The root app/CLI tests are an independent parallel gate step and need their own SwiftPM build tree.
.build-app-tests/
# Scratch output of docs/research/35-ios-remote-client/ios-cross-compile.sh: one
# SwiftPM tree per package per iOS triple, plus the per-module build logs.
.build-ios-*/
.swiftpm/
Package.resolved
__pycache__/

# Reproducible external source checkouts (materialize with `just fetch-references`).
/references/
